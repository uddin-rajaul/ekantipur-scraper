[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Playwright scraper for ekantipur.com entertainment section"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.59.0",
]
